# Adobe University Hackathon 2026 — Round 3 Build Plan

Marketplace: brand-ai-readiness-audit
Rule: one step at a time, verify "Done when" before moving on. No coding until told to start.

## Phase 0: Foundations

- [x] **Step 1. Project skeleton and manifest.** (done 2026-09-07) Marketplace root, marketplace.json with 5 skills and 1 entrypoint, 5 minimal SKILL.md files with valid frontmatter, empty scripts/ and references/ folders, MIT license, placeholder README.
  Done when: skills-ref validator passes on all five skills.

- [x] **Step 2. Shared conventions document.** (done 2026-09-07; 7 files in skills/audit-orchestrator/references/, fetch_policy.md added) Reference files: probe finding schema, severity + confidence rubric, bot tier list (live-answer / index / training-only), site category definitions, mechanism coverage map A–F, check_id naming rule.
  Done when: every later step points to one of these instead of inventing rules.

- [x] **Step 3. Test fixtures.** (done 2026-09-07; 9 fixtures in tests/fixtures/, blanket-disallow added; tests/serve_fixtures.py + tests/run_tests.py, 179 checks green) Static HTML: clean site, CSR shell, JS gate, bot-blocking robots, malformed JSON-LD, image-only pricing, non-HTML seed, minimal one-page portfolio. Local server script + test runner stub.
  Done when: fixtures serve locally.

## Phase 1: Fetch layer

- [x] **Step 4. Shared fetch helper.** (done 2026-09-07; skills/audit-orchestrator/scripts/auditlib/{fetch,robots}.py + fetch_url.py CLI; challenge-page fixture added; 280 tests green incl. example.com + python.org live) GET only, timeouts, redirect cap, byte cap, one retry on 429, content-type check, challenge-page fingerprinting, robots.txt parsing with bot tiers. Never raises.
  Done when: runs on every fixture and two live sites with no traceback.

- [x] **Step 5. Page sampler and site categorizer.** (done 2026-09-07; auditlib/htmldoc.py + auditlib/sampler.py + sample_site.py CLI; all 10 fixtures categorised correctly; 515 tests green; note for Step 19: python.org ties publisher_media/nonprofit at 4-4) Up to 6 pages by role (home, about, contact, pricing, product/service, blog/news) via nav keywords then sitemap. Infers category. Saves snapshots.
  Done when: correct sample manifest and category on every fixture.

## Phase 2: Probes (one at a time)

- [x] **Step 6. Crawl-render probe.** (done 2026-09-07; skills/crawl-render-audit/scripts/crawl_probe.py + auditlib/{findings,categories,context}.py; all 10 fixtures match their contracts; 605 tests green) robots by bot tier, blanket disallow, JS gate (incl. onload-form-submit gate; plain 3xx is never a gate), CSR shell (two-signal rule), sitemap, noindex, canonical, challenge inconclusive note.
  Done when: each fixture triggers exactly its expected findings; clean fixture triggers none.
- [x] **Step 7. Crawl-render SKILL.md + references.** (done 2026-09-07; SKILL.md with 6-step procedure; references/checks.md covers all 16 cr.* checks with rule/evidence/rationale; references/non_findings.md lists 12 explicit non-findings; hygiene test enforces references cover every registered id)

- [x] **Step 8. Fact-extractability probe.** (done 2026-09-07; skills/fact-extractability-audit/scripts/facts_probe.py + auditlib/{extract,render,cli}.py; all 12 fx.* checks; 11 fixtures match their contracts incl. new bare-metadata which covers the 7 previously unexercised fail paths; work/extracted_facts.json readable on fixtures and live (python.org, plausible.io); 780 tests green) Category-gated key facts (sales-led 'request a demo' satisfies pricing_or_trial; optional audience + differentiator facts), image-only fact proxies, JSON-LD parse + required props, malformed JSON-LD as own finding, title/H1/meta, Open Graph, alt text, FAQ-shaped content. Writes extracted-facts file.
  Done when: fixtures pass and facts file is readable.
- [x] **Step 9. Fact-extractability SKILL.md + references.** (done 2026-09-07; SKILL.md with 8-step procedure incl. the facts-file-only rule for the simulation; references/checks.md covers all 12 fx.* checks with rule/evidence/rationale and the REQUIRED_PROPS table; references/extracted_facts.md defines the facts file field by field with detection order per fact and the source vocabulary; references/non_findings.md lists 23 explicit non-findings and the limits of regex extraction; fx.jsonld.missing now notes Microdata/RDFa at medium confidence; 785 tests green)

- [x] **Step 10. Entity-freshness-corroboration probe.** (done 2026-09-07; skills/entity-freshness-corroboration-audit/scripts/entity_probe.py, all 12 ef.* checks; entity lookup is one robots-allowed Wikipedia article or Wikidata EntityData fetch (Wikidata's search API is robots-disallowed for generic agents and is never called), verified live on plausible.io, python.org, adobe.com; off-site spot-check reads work/offsite_mentions.json written by the agent step, else tool_unavailable note; two new fixtures weak-entity + undated-entity cover the 8 fail paths; --offline degrades to 12 not_evaluated with reasons and no error; 1050 tests green) sameAs, Wikidata lookup with timeout + fallback, NAP plain text, visible dates vs dateModified, press/newsroom page. Search spot-check = agent step with not-evaluated fallback.
  Done when: degrades cleanly with network disabled.
- [x] **Step 11. Entity-freshness-corroboration SKILL.md + references.** (done 2026-09-08; SKILL.md with a 9-step procedure incl. the entity-file-only rule and the off-site agent step; references/checks.md covers all 12 ef.* checks with rule/evidence/rationale and the lookup mode/outcome table; references/non_findings.md lists 25 explicit non-findings and the limits of one title lookup over six pages; references/entity_file.md defines work/entity.json and work/entity_lookup.json field by field with the reuse rule; references/offsite_spotcheck.md defines the agent step and the exact shape of work/offsite_mentions.json, capped at five searches to match the probe's suggested_offsite_queries and its own finding text (not the three first planned) and ten verbatim mentions; check_nap now skips the name component when the brand name was guessed from the host, as the registry always said, pinned by three new tests; the press_page_missing registry row now states the link-text, path-segment and feed rules the code applies; the hygiene stage now cross-checks references/*.md against SKILL.md for every skill; procedure walked live on plausible.io (discover lookup verified Q105731934; off-site re-run reused the lookup with no refetch); 1062 tests green) Mirror the fact-extractability skill: SKILL.md with a numbered procedure (run probe, read error, read checks, read findings, open work/entity.json, the off-site spot-check agent step, hand JSON to orchestrator unchanged); references/checks.md covering all 12 ef.* checks with rule / evidence / rationale; references/non_findings.md (a common-word brand name is never itself a finding; sales-led sites; social-only sameAs is low not medium; Wikidata search API never called; lookup is a title lookup, not a search); references/offsite_spotcheck.md defining the agent step: at most 3 queries, the exact shape of work/offsite_mentions.json, and the rule that the agent records only what a search tool returned, never what it remembers. Extend the run_tests.py reference-coverage hygiene stage to the ef prefix.
  Done when: validator passes, hygiene stage green, a fresh reader can run the probe and the spot-check from SKILL.md alone.

- [x] **Step 12. Engagement probe.** (done 2026-09-08; fixtures weak-engagement (soft-404 catch-all route, dead link, fixed newsletter overlay, vague two-word hero, no nav/CTA/viewport/lang/trust signals), weak-ecommerce (one-product shop: no breadcrumbs, no search, product page without related links, bare 404, 41 script tags) and blocked-links (three plain-403 footer links form a cluster note, nothing counted broken); skills/engagement-audit/scripts/engagement_probe.py with all 16 en.* checks on the probe_main pattern; auditlib/htmldoc.py now records markup positions, list membership, overlay text and stylesheet/script/article counts; CTA, trust-signal, category-noun and offer-verb vocabularies live in auditlib/categories.py; link sample of at most 15 with already-sampled pages reused, a deterministic 404-probe path, and --no-network/--offline degrading to network_disabled with a single info note; listing pages (articles or in-list links, sibling subdomains counted) and image-only h1s handled after a live check on python.org; a hygiene test asserts every en.* id is exercised by a fixture; live smoke on example.com (2 s) and python.org (11 s, 15 link requests) with no error; 1420 tests green) Two parts, fixtures first.
  12a. Fixtures: add `weak-engagement` (a saas site that fails en.mobile.viewport_missing, en.hero.value_prop_unclear, en.cta.missing, en.nav.landmark_missing, en.links.broken_sampled, en.lang.attribute_missing, en.errors.soft_404, en.interstitial.blocking, en.trust.signals_missing, en.continuity.h1_title_mismatch) and `weak-ecommerce` (a shop that fails en.nav.breadcrumbs_missing, en.nav.site_search_missing, en.nav.related_links_missing, en.errors.unhelpful_404, en.perf.page_weight_heavy via 41 script tags) so every en.* fail path is exercised at least once; a `blocked-links` variant where sampled internal links return 403 to exercise en.links.blocked_cluster. clean-site must pass all 16; one-page-portfolio must show the category not_evaluated set already listed in its notes.
  12b. Probe `skills/engagement-audit/scripts/engagement_probe.py` on the probe_main pattern. Network in workdir mode exactly as the entity probe does it (`ctx.fetcher or Fetcher(workdir, site)`), spending only the fetch-policy budget lines for the link sample (≤ 15) and the 404 probe (1); `--offline` makes en.links.* and en.errors.* not_evaluated with reason network_disabled. Apply the category caps table (site_categories.md section 4) and the CTA / trust vocabularies from that file; move those vocabularies into auditlib/categories.py so the file and the code cannot drift. Hero and CTA are structural (first 40% of body markup, first 120 words), confidence per the registry (hero low unless two signals).
  Done when: all 16 en.* ids have a fixture assertion, clean-site and one-page-portfolio stay green, probe_en stage green, offline run degrades without error.
- [x] **Step 13. Engagement SKILL.md + references.** (done 2026-09-08; SKILL.md with an 8-step procedure, the two-tier page rule (content checks on usable pages, head checks on every served page) and the full reason vocabulary; references/checks.md covers all 16 en.* checks with rule/evidence/rationale, the category applicability table and the CTA and trust-signal vocabularies by category; references/non_findings.md lists 23 explicit non-findings and what a non-rendering, English-first audit cannot know; references/engagement_file.md defines work/engagement.json field by field with a worked example; validator green; hygiene stage cross-checks references against SKILL.md and every en.* id; 1425 tests green) Same shape as Step 11: procedure, references/checks.md for all 16 en.* checks, references/non_findings.md (a one-page site is not disoriented; icon-only or non-English CTAs are a stated limit; page weight is a proxy; lazy embeds; a 403 cluster is not broken links). Hygiene stage extended to the en prefix.
  Done when: validator and hygiene green.

## Phase 3: Orchestrator

All three scripts live in `skills/audit-orchestrator/scripts/`. Every one is stdlib-only and never raises.

- [x] **Step 14. Compose script (`compose.py`).** Input: a workdir with sample.json and probes/*.json. Merge; apply the dedupe table (coverage_map.md section 4) in order with severity inheritance; sort per report_schema.md section 4; assign F-### ids and rank; counts; quick wins; derived tags (handout_concepts, pipeline_stage, opportunity_type, round2_mode, impact/priority); passed_checks with positive titles; coverage block from section 6 of the coverage map; limitations boilerplate (point-in-time with page count, only-home-sampled, 429 skips, challenge pages, served-HTML-only, plus section 5 non-coverage); proactive_recommendations chosen by category and by which checks passed (a recommendation is never a restated finding); the `ai_answer_simulation` block **pre-filled deterministically** from work/extracted_facts.json (questions from site_categories.md section 6, `answerable` and `missing_facts` computed, `answer_from_facts` left null for the agent, `basis` fixed); `or.simulation.question_unanswerable` and `or.run.probe_error` findings; `narrative_summary` left as an empty string for the agent. Render report.md from the JSON (section 6 rules) in the same script.
  Done when: clean-site yields a positive report with empty findings and populated proactive recommendations; every broken fixture yields the counts its _fixture.json implies after dedupe; the Markdown contains nothing the JSON does not.
  (done 2026-09-08; `skills/audit-orchestrator/scripts/compose.py`, 780 lines: merge, the 12-row dedupe table with severity inheritance, ordering + F-### ids + rank, counts, quick wins, the four derived tags, `passed_checks` from a `POSITIVE_TITLES` table covering all 58 registry rows, the coverage block with a filled section-6 statement, limitations that read coverage_map section 5 from the file itself, 13 proactive recommendations gated so none restates a finding, the deterministically pre-filled simulation (Q1-Q3 per category plus the fit and comparison questions, from `SIMULATION_QUESTIONS`/`AUDIENCE_PHRASE` new in categories.py), both `or.*` findings built through ProbeOutput, and the Markdown renderer with `--render-only`. Reference edits so code and conventions agree: coverage_map rules (an inconclusive primary never inherits severity; entry-level rows 8/9 fold only on full containment), check_ids (positive titles), site_categories section 6 (the {audience} placeholder table). findings.py now snaps over-long titles to a word boundary. New test stage `compose`: 12 synthetic dedupe cases, ordering, tags, quick-win rule, table-vs-registry hygiene, and a full report per fixture. Suite 2176 green.)
- [x] **Step 15. Validate script (`validate.py`).** Checks the required floor (site, audited_at, summary counts that add up and match findings length, findings with id/title/severity/evidence/suggested_action.summary+priority), then the superset (enum values, unique ids, ids in registry, dedupe integrity, `basis == extracted_facts_only`, no simulation answer that cites a fact absent from the facts file, narrative non-empty when `--final`). Exit 0/1 with a list of problems.
  Done when: rejects each of a set of deliberately broken reports and accepts every fixture report.
  (done 2026-09-08; `skills/audit-orchestrator/scripts/validate.py`, 330 lines. `validate_report(report, facts, final) -> (problems, warnings)`; problems are tagged `floor:` or `superset:` so a handout-shape failure is never confused with a convention failure. Floor: site, audited_at, the six summary counts adding up and matching the findings per severity, and the five per-finding fields. Superset: every enum, mechanism and max severity against the registry, schema ordering, sequential ids and ranks, the quick-win rule, impact/priority derivation, dedupe_key shape, evidence-item kinds and lengths, passed_checks never also a finding, dedupe integrity (merged_into exists, merged_from lists it, no key both kept and folded), the coverage shape, recommendations never restating a finding title, the limitations boilerplate, and the simulation contract: basis fixed, no answer on an unanswerable question, every fact in facts_used present in the facts file, and the answer carrying a verbatim 20-character run of each fact's value. `--final` also requires a narrative and an answer for every answerable question; sentence count and attribution_note are warnings. CLI resolves the facts file from the report's own `facts_file` path. New test stage `validate`: 45 deliberately broken reports each rejected with the expected problem and tier, a mechanically finalised report accepted under --final, garbage input rejected without a crash; every fixture report is also validated inside the compose stage. Suite 2296 green.)
- [x] **Step 16. Run-audit script (`run_audit.py`).** One command: fetch once via the sampler, run the four probes on the workdir in stage order, compose, validate (non-final), write report.json + report.md, print the path. 300 s wall clock enforced through the Fetcher time budget and per-probe 60 s; top-level guard emits a one-finding "site unreachable" report on any failure. `--offline`, `--category`, `--workdir` pass through.
  Done when: completes on every fixture and on three live sites of different categories in under 5 minutes each, with no traceback in any output.
  (done 2026-09-08; `skills/audit-orchestrator/scripts/run_audit.py`, 210 lines. Samples once, then runs each probe as its own **subprocess** so the 60 s per-probe limit is actually enforceable; a probe that crashes, hangs, exits non-zero or prints a traceback is replaced by a stub output carrying the reason, so the run continues and `or.run.probe_error` names it. The 300 s wall clock is split: 55 % to the sampler via the Fetcher's existing `time_budget` (new `time_budget` argument on `AuditContext.from_url`), then `min(60, remaining - 5)` per probe, and a probe with under 8 s left is skipped rather than started and killed. Composes, validates non-final, records `run.probes`, `wall_clock_seconds`, `time_budget_seconds` and any validation problems, writes both files. Two real bugs it surfaced, both fixed: **`--offline` was ignored in workdir mode** by the engagement and entity probes (they consulted `ctx.fetcher`, which is None there, and went to the network anyway; replaying a saved workdir offline counted every unreachable link as broken), and **`--category` graded the probes but the report still printed the inferred category** (compose now takes `category_override` and states it with confidence high and signal `override:command_line`). New test stage `run_audit`: the CLI end to end on all 16 fixtures (exit 0, no traceback, valid report, fail set reproduced, all four probes complete, under 60 s each), plus workdir reuse without re-sampling, `--offline`, `--no-network`, `--category`, a starved budget, a refused connection, and injected crashing and hanging probes. Live: example.com 4.3 s, python.org 13.3 s, gutenberg.org 21.4 s, three categories, no traceback. Suite 2478 green.)
- [ ] **Step 17. Orchestrator SKILL.md.** The procedure a fresh agent follows: run run_audit.py; open report.json; for each simulation question with `answerable: true`, write `answer_from_facts` quoting only the verbatim `value` fields from the facts file and listing `facts_used`; for `false`, leave the answer null and point at the finding id; write `attribution_note`; run the off-site spot-check from the entity skill's reference if a search tool exists, else leave the not_evaluated note; write the narrative (3–6 sentences, grouped invisible / stale / bouncing); re-run compose's Markdown render and validate.py --final; emit both files. References: simulation_rules.md (the facts-only rule, the forbidden-knowledge rule, wording), and pointers to schema, rubric, coverage map.
  Done when: a fresh agent session given only the SKILL.md and a URL produces a report that passes validate.py --final.

## Phase 4: Hardening and proof

- [ ] **Step 18. Test runner.** Add stages probe_en, compose, validate, run_audit (end to end on every fixture), and a traceback grep over every script's stdout/stderr. Keep the existing 1050 green.
  Done when: one command, green.
- [ ] **Step 19. Live generalization pass.** 8–10 unseen sites: one per category, plus one non-English, one bot-protected, one unreachable domain, one bare IP or non-HTML seed. Read every finding and ask "would I defend this to an Adobe judge". Fix rules, not sites. Resolve the python.org publisher_media / nonprofit tie noted in Step 5 by a documented tiebreak rule, not a special case.
  Done when: every finding on every site is defensible and each run is under 5 minutes.
- [ ] **Step 20. Source verification.** Open every citation and Adobe fact with a web tool. Keep only what is confirmed, with URLs, in `references/sources.md`. Must verify before use: every bot_tiers.md row (vendor docs); arXiv 2603.09296 (citation-failure taxonomy) and arXiv 2605.14021 (AI Overviews study), both taken from the strategy docs and unconfirmed; Ahrefs 2026 schema/citation study; Princeton/IIT Delhi GEO study KDD 2024; NNG 57% above-the-fold; Adobe Analytics AI-referred traffic +1,324% retail / +2,215% travel; Gary Illyes on llms.txt (Jul 2025); bot-block prevalence stats (GPTBot 5.5% / 25% top-1000 / 41% B2B); RFC 9309; Google JS SEO basics; every URL in the probes' REFS dicts. Anything unconfirmed is removed from the code, references and README, not softened.
  Done when: sources.md has no unverified entries and no script or reference cites anything outside it.

## Phase 5: Packaging

- [ ] **Step 21. README.** What it does (agentic vs referral traffic framing; inside-out vs outside-in positioning statement), skill table, composition diagram, how to run (one command plus per-skill standalone), report schema, coverage map and limitations (generated from coverage_map.md), sources (verified only), prior-art section (free checkers, monitors, enterprise GEO, claude-seo) and how this differs, field-research note. Replace the placeholder `+URL` in the fetch user agent with the real repository URL.
  Done when: a stranger can run it from the README alone.
- [ ] **Step 22. Demo report.** A neutral live site, JSON + Markdown in `examples/`, produced by the Step 17 procedure end to end (agent-written simulation and narrative included), so the example shows the finished shape.
- [ ] **Step 23. Final compliance sweep.** Validator on all skills, manifest with exactly one entrypoint, zip size, `grep` for non-stdlib imports and for any non-GET method, robots respected, runtime confirmed on two sites, no tests/ or workdirs in the zip unless intended. Zip the marketplace root.
- [ ] **Step 24. Dry-run judging.** Read the zip against each rubric line as a judge would. Fix weak spots. Submit.

## Working rules from here

- Commit after every step with the step number in the message (the repo has one commit so far; everything through Step 10 is in it).
- Every step ends with `python3 tests/run_tests.py` green before it is ticked.
- Nothing in a SKILL.md or reference cites a source that Step 20 has not confirmed; until then, mechanism explanations stay in plain language without numbers.
